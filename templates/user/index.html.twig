{% extends 'layouts/base.html.twig' %}

{% block metaTitle 'user.index.title'|trans %}

{% block body %}
    <h1 class="font-bold text-2xl mb-4 mt-4 text-center">{{ 'user.index.title'|trans }}</h1>

    <p class="mb-8 text-center">
        {{ 'user.index.description'|trans }}
    </p>

    <!-- Accéder à la liste des favoris -->
    <div class="flex justify-center">
        <a href="{{ path('user_favorite_list') }}" class="btn btn-primary">{{ 'user.index.favorites'|trans }}</a>
    </div>
{% endblock %}
