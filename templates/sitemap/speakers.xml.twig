<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for speaker in speakers %}
        <url>
            <loc>
                {{ url('speaker_show', { slug: speaker.slug }) }}
            </loc>
            <lastmod>{{ 'now'|date('Y-m-d') }}</lastmod>
        </url>
    {% endfor %}
</urlset>