<Like
    data-like
    data-like-type="{{ type }}"
    data-like-id="{{ id }}"
    type="{{ type }}"
    id="{{ id }}"
    name="{{ name }}"
    :focusable="{{ focusable ? 'true' : 'false' }}"
>
    {{ ux_icon('lucide:heart') }}
</Like>
